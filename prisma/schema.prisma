generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url = "postgresql://username:password@localhost:5173/database"
}


model User {
  user            String @id
  hashedPassword  String
}
